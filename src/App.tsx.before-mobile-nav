import { createPortal } from "react-dom"
import { useEffect, useState } from "react"
import type { ReactNode } from "react"
import { AuthProvider, useAuth } from "./auth/AuthProvider"
import Login from "./pages/Login"
import { supabase } from "./lib/supabase"
import { BrowserRouter, Navigate, NavLink, Route, Routes, useNavigate, useParams } from "react-router-dom"
import * as mammoth from "mammoth"
import {
  Archive,
  BriefcaseBusiness,
  Download,
  Eye,
  FileText,
  FolderOpen,
  LayoutDashboard,
  Pencil,
  Trash2,
  LogOut,
  Search,
  Upload,
  Users,
} from "lucide-react"

function Sidebar() {
  const { user, signOut } = useAuth()

  async function handleSignOut() {
    try {
      await signOut()
    } catch (error) {
      console.error("Gagal logout:", error)
    }
  }
  return (
    <aside className="sidebar">
      <div className="brand">
        <div className="brand-mark">
          <Archive size={22} strokeWidth={2} />
        </div>

        <div>
          <div className="brand-title">LEMARI ARSIP</div>
          <div className="brand-subtitle">DIGITAL LAWYER</div>
        </div>
      </div>

      <nav className="navigation">
        <div className="nav-label">MENU UTAMA</div>

        <NavLink
          to="/"
          end
          className={({ isActive }) =>
            `nav-item ${isActive ? "active" : ""}`
          }
        >
          <LayoutDashboard size={19} />
          <span>Dashboard</span>
        </NavLink>

        <NavLink
          to="/klien"
          className={({ isActive }) =>
            `nav-item ${isActive ? "active" : ""}`
          }
        >
          <Users size={19} />
          <span>Klien</span>
        </NavLink>

        <NavLink
          to="/perkara"
          className={({ isActive }) =>
            `nav-item ${isActive ? "active" : ""}`
          }
        >
          <BriefcaseBusiness size={19} />
          <span>Perkara</span>
        </NavLink>

        <NavLink
          to="/berkas"
          className={({ isActive }) =>
            `nav-item ${isActive ? "active" : ""}`
          }
        >
          <FolderOpen size={19} />
          <span>Berkas</span>
        </NavLink>
      </nav>

      <div className="sidebar-footer">
        <div className="sidebar-profile">
          <div className="profile-avatar">
            {(user?.email?.[0] ?? "L").toUpperCase()}
          </div>

          <div className="sidebar-profile-info">
            <strong>{user?.email?.split("@")[0] ?? "Lawyer"}</strong>
            <span>{user?.email ?? "Pengguna"}</span>
          </div>
        </div>

        <button
          type="button"
          className="logout-button"
          onClick={handleSignOut}
          title="Keluar"
        >
          <LogOut size={17} />
          <span>Keluar</span>
        </button>
      </div>
    </aside>
  )
}

function Topbar({
  title,
  eyebrow = "DIGITAL CASE FILE",
  action,
}: {
  title: string
  eyebrow?: string
  action?: ReactNode
}) {
  return (
    <header className="topbar">
      <div>
        <p className="eyebrow">{eyebrow}</p>
        <h1>{title}</h1>
      </div>

      <div className="topbar-right">
        {action}

        <div className="topbar-profile">
          <div className="profile-avatar">P</div>

          <div>
            <strong>Lawyer</strong>
            <span>Administrator</span>
          </div>
        </div>
      </div>
    </header>
  )
}

function PageLayout({
  children,
}: {
  children: ReactNode
}) {
  return (
    <div className="app-shell">
      <Sidebar />

      <main className="main-content">
        {children}
      </main>
    </div>
  )
}

function Dashboard() {
  const { user } = useAuth()

  const [clientCount, setClientCount] = useState(0)
  const [caseCount, setCaseCount] = useState(0)
  const [documentCount, setDocumentCount] = useState(0)
  const [loadingStats, setLoadingStats] = useState(true)

  useEffect(() => {
    async function loadDashboardStats() {
      if (!user) {
        setClientCount(0)
        setCaseCount(0)
        setDocumentCount(0)
        setLoadingStats(false)
        return
      }

      setLoadingStats(true)

      const [
        clientsResult,
        casesResult,
        documentsResult,
      ] = await Promise.all([
        supabase
          .from("clients")
          .select("id", { count: "exact", head: true })
          .eq("lawyer_id", user.id),

        supabase
          .from("cases")
          .select("id", { count: "exact", head: true })
          .eq("lawyer_id", user.id),

        supabase
          .from("documents")
          .select("id", { count: "exact", head: true })
          .eq("lawyer_id", user.id),
      ])

      if (clientsResult.error) {
        console.error("Gagal mengambil jumlah klien:", clientsResult.error)
      }

      if (casesResult.error) {
        console.error("Gagal mengambil jumlah perkara:", casesResult.error)
      }

      if (documentsResult.error) {
        console.error("Gagal mengambil jumlah berkas:", documentsResult.error)
      }

      setClientCount(clientsResult.count ?? 0)
      setCaseCount(casesResult.count ?? 0)
      setDocumentCount(documentsResult.count ?? 0)

      setLoadingStats(false)
    }

    void loadDashboardStats()
  }, [user])

  return (
    <PageLayout>
      <Topbar title="Dashboard" />

      <section className="welcome-section">
        <div>
          <p className="eyebrow">SELAMAT DATANG</p>

          <h2>
            Semua berkas perkara,
            <br />
            tersusun dalam satu tempat.
          </h2>

          <p className="welcome-text">
            Simpan, temukan, dan akses berkas perkara Anda dengan mudah
            kapan saja dari perangkat apa pun.
          </p>
        </div>

        <div className="welcome-icon">
          <Archive size={58} strokeWidth={1.2} />
        </div>
      </section>

      <section className="search-section">
        <Search size={21} />

        <input
          type="text"
          placeholder="Cari nama klien, perkara, atau berkas..."
        />

        <span className="search-shortcut">Ctrl K</span>
      </section>

      <section className="stats-grid">
        <div className="stat-card">
          <div className="stat-icon">
            <Users size={22} />
          </div>

          <div>
            <span>Total Klien</span>
            <strong>
              {loadingStats ? "..." : clientCount}
            </strong>
          </div>
        </div>

        <div className="stat-card">
          <div className="stat-icon">
            <BriefcaseBusiness size={22} />
          </div>

          <div>
            <span>Total Perkara</span>
            <strong>
              {loadingStats ? "..." : caseCount}
            </strong>
          </div>
        </div>

        <div className="stat-card">
          <div className="stat-icon">
            <FileText size={22} />
          </div>

          <div>
            <span>Total Berkas</span>
            <strong>
              {loadingStats ? "..." : documentCount}
            </strong>
          </div>
        </div>
      </section>

      <section className="content-card">
        <div className="section-heading">
          <div>
            <p className="eyebrow">ARSIP</p>
            <h3>Klien Terbaru</h3>
          </div>
        </div>

        <div className="empty-state">
          <div className="empty-icon">
            <FolderOpen size={30} />
          </div>

          <h4>
            {clientCount > 0
              ? `${clientCount} klien tersimpan`
              : "Belum ada klien"}
          </h4>

          <p>
            {clientCount > 0
              ? "Data klien Anda sudah tersimpan di lemari arsip digital."
              : "Tambahkan klien pertama untuk mulai membangun lemari arsip digital Anda."}
          </p>

          <NavLink to="/klien" className="primary-button">
            {clientCount > 0 ? "Kelola Klien" : "+ Tambah Klien"}
          </NavLink>
        </div>
      </section>
    </PageLayout>
  )
}
function Clients() {
  const { user } = useAuth()

  type Client = {
    id: string
    name: string
    identity_number: string | null
    phone: string | null
    email: string | null
  }

  const [clients, setClients] = useState<Client[]>([])
  const [search, setSearch] = useState("")
  const [showModal, setShowModal] = useState(false)
  const [editingClient, setEditingClient] = useState<Client | null>(null)
  const [loading, setLoading] = useState(true)
  const [saving, setSaving] = useState(false)
  const [deletingId, setDeletingId] = useState<string | null>(null)
  const [error, setError] = useState("")

  const [name, setName] = useState("")
  const [identityNumber, setIdentityNumber] = useState("")
  const [phone, setPhone] = useState("")
  const [email, setEmail] = useState("")

  async function loadClients() {
    if (!user) return

    setLoading(true)
    setError("")

    const { data, error } = await supabase
      .from("clients")
      .select("id, name, identity_number, phone, email")
      .eq("lawyer_id", user.id)
      .order("created_at", { ascending: false })

    if (error) {
      console.error(error)
      setError(error.message)
      setClients([])
    } else {
      setClients(data ?? [])
    }

    setLoading(false)
  }

  useEffect(() => {
    void loadClients()
  }, [user])

  function resetForm() {
    setName("")
    setIdentityNumber("")
    setPhone("")
    setEmail("")
    setError("")
  }

  function openAddModal() {
    setEditingClient(null)
    resetForm()
    setShowModal(true)
  }

  function openEditModal(client: Client) {
    setEditingClient(client)
    setName(client.name)
    setIdentityNumber(client.identity_number ?? "")
    setPhone(client.phone ?? "")
    setEmail(client.email ?? "")
    setError("")
    setShowModal(true)
  }

  function closeModal() {
    if (saving) return

    setShowModal(false)
    setEditingClient(null)
    resetForm()
  }

  async function handleSubmit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault()

    if (!user) {
      setError("Sesi login tidak ditemukan.")
      return
    }

    if (!name.trim()) {
      setError("Nama lengkap wajib diisi.")
      return
    }

    setSaving(true)
    setError("")

    if (editingClient) {
      const { error } = await supabase
        .from("clients")
        .update({
          name: name.trim(),
          identity_number: identityNumber.trim() || null,
          phone: phone.trim() || null,
          email: email.trim() || null,
        })
        .eq("id", editingClient.id)
        .eq("lawyer_id", user.id)

      if (error) {
        console.error(error)
        setError(error.message)
        setSaving(false)
        return
      }
    } else {
      const { error } = await supabase.from("clients").insert({
        lawyer_id: user.id,
        name: name.trim(),
        identity_number: identityNumber.trim() || null,
        phone: phone.trim() || null,
        email: email.trim() || null,
      })

      if (error) {
        console.error(error)
        setError(error.message)
        setSaving(false)
        return
      }
    }

    await loadClients()

    setSaving(false)
    setShowModal(false)
    setEditingClient(null)
    resetForm()
  }

  async function handleDelete(client: Client) {
    if (!user) {
      setError("Sesi login tidak ditemukan.")
      return
    }

    const confirmed = window.confirm(
      `Yakin ingin menghapus klien "${client.name}"?`
    )

    if (!confirmed) return

    setDeletingId(client.id)
    setError("")

    const { error } = await supabase
      .from("clients")
      .delete()
      .eq("id", client.id)
      .eq("lawyer_id", user.id)

    if (error) {
      console.error(error)
      setError(error.message)
      setDeletingId(null)
      return
    }

    await loadClients()
    setDeletingId(null)
  }

  const filteredClients = clients.filter((client) => {
    const keyword = search.trim().toLowerCase()

    if (!keyword) return true

    return (
      client.name.toLowerCase().includes(keyword) ||
      (client.identity_number ?? "").toLowerCase().includes(keyword) ||
      (client.phone ?? "").toLowerCase().includes(keyword) ||
      (client.email ?? "").toLowerCase().includes(keyword)
    )
  })

  return (
    <PageLayout>
      <Topbar
        title="Klien"
        eyebrow="DATA KLIEN"
        action={
          <button
            type="button"
            className="primary-button"
            onClick={openAddModal}
          >
            + Tambah Klien
          </button>
        }
      />

      <section className="page-intro">
        <p className="eyebrow">LEMARI ARSIP</p>

        <h2>Daftar Klien</h2>

        <p>
          Semua klien tersimpan rapi bersama perkara dan berkas
          yang terkait.
        </p>
      </section>

      <section className="document-toolbar">
        <div className="document-search">
          <Search size={19} />

          <input
            type="text"
            value={search}
            onChange={(event) => setSearch(event.target.value)}
            placeholder="Cari nama, NIK, nomor HP, atau email..."
          />
        </div>
      </section>

      {error && !showModal && (
        <div className="client-page-error">
          {error}
        </div>
      )}

      <section className="document-card clients-card">
        <div className="document-card-header">
          <div>
            <h3>Daftar Klien</h3>
            <span>
              {loading
                ? "Memuat data..."
                : `${filteredClients.length} klien`}
            </span>
          </div>
        </div>

        {loading ? (
          <div className="empty-state">
            <h4>Memuat data klien...</h4>
          </div>
        ) : filteredClients.length === 0 ? (
          <div className="empty-state">
            <div className="empty-icon">
              <Users size={30} />
            </div>

            <h4>
              {search ? "Klien tidak ditemukan" : "Belum ada klien"}
            </h4>

            <p>
              {search
                ? "Coba gunakan kata kunci pencarian yang berbeda."
                : "Tambahkan klien pertama untuk mulai membangun lemari arsip digital Anda."}
            </p>

            {!search && (
              <button
                type="button"
                className="primary-button"
                onClick={openAddModal}
              >
                + Tambah Klien
              </button>
            )}
          </div>
        ) : (
          <div className="clients-table-wrap">
            <table className="clients-table">
              <thead>
                <tr>
                  <th>Nama Klien</th>
                  <th>NIK / Identitas</th>
                  <th>Nomor HP</th>
                  <th>Email</th>
                  <th>Aksi</th>
                </tr>
              </thead>

              <tbody>
                {filteredClients.map((client) => (
                  <tr key={client.id}>
                    <td>
                      <strong>{client.name}</strong>
                    </td>
                    <td>{client.identity_number || "—"}</td>
                    <td>{client.phone || "—"}</td>
                    <td>{client.email || "—"}</td>
                    <td>
                      <div className="client-actions">
                        <button
                          type="button"
                          className="client-action-button edit"
                          onClick={() => openEditModal(client)}
                          title="Edit klien"
                        >
                          <Pencil size={15} />
                          <span>Edit</span>
                        </button>

                        <button
                          type="button"
                          className="client-action-button delete"
                          onClick={() => void handleDelete(client)}
                          disabled={deletingId === client.id}
                          title="Hapus klien"
                        >
                          <Trash2 size={15} />
                          <span>
                            {deletingId === client.id
                              ? "Menghapus..."
                              : "Hapus"}
                          </span>
                        </button>
                      </div>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}
      </section>

      {showModal && (
        <div className="modal-backdrop" onMouseDown={closeModal}>
          <div
            className="client-modal"
            onMouseDown={(event) => event.stopPropagation()}
          >
            <div className="client-modal-header">
              <div>
                <p className="eyebrow">DATA KLIEN</p>
                <h3>
                  {editingClient ? "Edit Klien" : "Tambah Klien"}
                </h3>
              </div>

              <button
                type="button"
                className="modal-close"
                onClick={closeModal}
                disabled={saving}
                aria-label="Tutup"
              >
                ×
              </button>
            </div>

            <form onSubmit={handleSubmit} className="client-form">
              <label>
                Nama Lengkap *
                <input
                  type="text"
                  value={name}
                  onChange={(event) => setName(event.target.value)}
                  placeholder="Masukkan nama lengkap"
                  autoFocus
                  required
                />
              </label>

              <label>
                NIK / Nomor Identitas
                <input
                  type="text"
                  value={identityNumber}
                  onChange={(event) =>
                    setIdentityNumber(event.target.value)
                  }
                  placeholder="Masukkan NIK atau nomor identitas"
                />
              </label>

              <label>
                Nomor HP
                <input
                  type="tel"
                  value={phone}
                  onChange={(event) => setPhone(event.target.value)}
                  placeholder="Contoh: 081234567890"
                />
              </label>

              <label>
                Email
                <input
                  type="email"
                  value={email}
                  onChange={(event) => setEmail(event.target.value)}
                  placeholder="Masukkan email"
                />
              </label>

              {error && (
                <div className="client-form-error" role="alert">
                  {error}
                </div>
              )}

              <div className="client-form-actions">
                <button
                  type="button"
                  className="secondary-button"
                  onClick={closeModal}
                  disabled={saving}
                >
                  Batal
                </button>

                <button
                  type="submit"
                  className="primary-button"
                  disabled={saving}
                >
                  {saving
                    ? "Menyimpan..."
                    : editingClient
                      ? "Simpan Perubahan"
                      : "Simpan Klien"}
                </button>
              </div>
            </form>
          </div>
        </div>
      )}
    </PageLayout>
  )
}
function Cases() {
  const { user } = useAuth()
  const navigate = useNavigate()

  const [clients, setClients] = useState<
    Array<{ id: string; name: string }>
  >([])

  const [environments, setEnvironments] = useState<
    Array<{ id: string; name: string }>
  >([])

  const [courtTypes, setCourtTypes] = useState<
    Array<{ id: string; name: string; judicial_environment_id: string }>
  >([])

  const [courts, setCourts] = useState<
    Array<{
      id: string
      name: string
      court_type_id: string
    }>
  >([])

  const [caseTypes, setCaseTypes] = useState<
    Array<{ id: string; name: string; court_type_id: string }>
  >([])

  const [caseSubtypes, setCaseSubtypes] = useState<
    Array<{ id: string; name: string; case_type_id: string }>
  >([])

  const [cases, setCases] = useState<
    Array<{
      id: string
      client_id: string
      judicial_environment_id: string
      court_type_id: string
      court_id: string
      case_type_id: string
      case_subtype_id: string | null
      case_number: string
      title: string
      status: string | null
      created_at: string
    }>
  >([])

  const [search, setSearch] = useState("")
  const [showModal, setShowModal] = useState(false)
  const [loading, setLoading] = useState(true)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState("")

  const [selectedClientId, setSelectedClientId] = useState("")
  const [selectedEnvironmentId, setSelectedEnvironmentId] = useState("")
  const [selectedCourtTypeId, setSelectedCourtTypeId] = useState("")
  const [selectedCourtId, setSelectedCourtId] = useState("")
  const [selectedCaseTypeId, setSelectedCaseTypeId] = useState("")
  const [selectedSubtypeId, setSelectedSubtypeId] = useState("")
  const [caseNumber, setCaseNumber] = useState("")
  const [caseTitle, setCaseTitle] = useState("")

  useEffect(() => {
    async function loadMasterData() {
      if (!user) return

      setLoading(true)
      setError("")

      const [
        clientsResult,
        environmentsResult,
        courtTypesResult,
        courtsResult,
        caseTypesResult,
        caseSubtypesResult,
      ] = await Promise.all([
        supabase
          .from("clients")
          .select("id, name")
          .eq("lawyer_id", user.id)
          .order("name", { ascending: true }),

        supabase
          .from("judicial_environments")
          .select("id, name")
          .order("name", { ascending: true }),

        supabase
          .from("court_types")
          .select("id, name, judicial_environment_id")
          .order("name", { ascending: true }),

        supabase
          .from("courts")
          .select("id, name, court_type_id")
          .order("name", { ascending: true }),

        supabase
          .from("case_types")
          .select("id, name, court_type_id")
          .order("name", { ascending: true }),

        supabase
          .from("case_subtypes")
          .select("id, name, case_type_id")
          .order("name", { ascending: true }),
      ])

      const masterErrors = [
        ["clients", clientsResult.error],
        ["judicial_environments", environmentsResult.error],
        ["court_types", courtTypesResult.error],
        ["courts", courtsResult.error],
        ["case_types", caseTypesResult.error],
        ["case_subtypes", caseSubtypesResult.error],
      ] as const

      const failedMaster = masterErrors.find(([, error]) => error)

      if (failedMaster) {
        const [tableName, masterError] = failedMaster

        console.error(`Gagal memuat master ${tableName}:`, masterError)

        setError(
          `Gagal memuat ${tableName}: ${masterError?.message ?? "Unknown error"}`
        )

        setLoading(false)
        return
      }

      setClients(clientsResult.data ?? [])
      setEnvironments(environmentsResult.data ?? [])
      setCourtTypes(courtTypesResult.data ?? [])
      setCourts(courtsResult.data ?? [])
      setCaseTypes(caseTypesResult.data ?? [])
      setCaseSubtypes(caseSubtypesResult.data ?? [])

      setLoading(false)
    }

    void loadMasterData()
  }, [user])

  async function loadCases() {
    if (!user) return

    const { data, error: loadError } = await supabase
      .from("cases")
      .select(
        "id, client_id, judicial_environment_id, court_type_id, court_id, case_type_id, case_subtype_id, case_number, title, status, created_at"
      )
      .eq("lawyer_id", user.id)
      .order("created_at", { ascending: false })

    if (loadError) {
      console.error(loadError)
      setError("Gagal memuat daftar perkara.")
      return
    }

    setCases(data ?? [])
  }

  useEffect(() => {
    void loadCases()
  }, [user])

  const filteredCourtTypes = courtTypes.filter(
    (item) =>
      !selectedEnvironmentId ||
      item.judicial_environment_id === selectedEnvironmentId
  )

  const filteredCourts = courts.filter(
    (item) =>
      !selectedCourtTypeId ||
      item.court_type_id === selectedCourtTypeId
  )

  const filteredCaseTypes = caseTypes.filter(
    (item) =>
      !selectedCourtTypeId ||
      item.court_type_id === selectedCourtTypeId
  )

  const filteredSubtypes = caseSubtypes.filter(
    (item) =>
      !selectedCaseTypeId ||
      item.case_type_id === selectedCaseTypeId
  )

  function resetForm() {
    setSelectedClientId("")
    setSelectedEnvironmentId("")
    setSelectedCourtTypeId("")
    setSelectedCourtId("")
    setSelectedCaseTypeId("")
    setSelectedSubtypeId("")
    setCaseNumber("")
    setCaseTitle("")
    setError("")
  }

  function openAddModal() {
    resetForm()
    setShowModal(true)
  }

  function closeModal() {
    if (saving) return

    setShowModal(false)
    resetForm()
  }

  function handleEnvironmentChange(value: string) {
    setSelectedEnvironmentId(value)
    setSelectedCourtTypeId("")
    setSelectedCourtId("")
    setSelectedCaseTypeId("")
    setSelectedSubtypeId("")
  }

  function handleCourtTypeChange(value: string) {
    setSelectedCourtTypeId(value)
    setSelectedCourtId("")
    setSelectedCaseTypeId("")
    setSelectedSubtypeId("")
  }

  function handleCaseTypeChange(value: string) {
    setSelectedCaseTypeId(value)
    setSelectedSubtypeId("")
  }

  async function handleSubmit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault()

    if (!user) return

    if (
      !selectedClientId ||
      !selectedEnvironmentId ||
      !selectedCourtTypeId ||
      !selectedCourtId ||
      !selectedCaseTypeId ||
      !caseNumber.trim() ||
      !caseTitle.trim()
    ) {
      setError(
        "Klien, peradilan, pengadilan, jenis perkara, nomor perkara, dan judul wajib diisi."
      )
      return
    }

    setSaving(true)
    setError("")

    const { error: insertError } = await supabase
      .from("cases")
      .insert({
        lawyer_id: user.id,
        client_id: selectedClientId,
        judicial_environment_id: selectedEnvironmentId,
        court_type_id: selectedCourtTypeId,
        court_id: selectedCourtId,
        case_type_id: selectedCaseTypeId,
        case_subtype_id: selectedSubtypeId || null,
        case_number: caseNumber.trim(),
        title: caseTitle.trim(),
      })

    if (insertError) {
      console.error(insertError)
      setError(insertError.message || "Gagal menyimpan perkara.")
      setSaving(false)
      return
    }

    await loadCases()

    setSaving(false)
    setShowModal(false)
    resetForm()
  }

  const getClientName = (clientId: string) =>
    clients.find((item) => item.id === clientId)?.name ?? "-"

  const getEnvironmentName = (id: string) =>
    environments.find((item) => item.id === id)?.name ?? "-"

  const getCourtTypeName = (id: string) =>
    courtTypes.find((item) => item.id === id)?.name ?? "-"

  const getCourtName = (id: string) =>
    courts.find((item) => item.id === id)?.name ?? "-"

  const getCaseTypeName = (id: string) =>
    caseTypes.find((item) => item.id === id)?.name ?? "-"

  const getSubtypeName = (id: string | null) =>
    caseSubtypes.find((item) => item.id === id)?.name ?? "-"

  const filteredCases = cases.filter((item) => {
    const keyword = search.toLowerCase().trim()

    if (!keyword) return true

    const searchableText = [
      getClientName(item.client_id),
      item.case_number,
      item.title,
      getEnvironmentName(item.judicial_environment_id),
      getCourtTypeName(item.court_type_id),
      getCourtName(item.court_id),
      getCaseTypeName(item.case_type_id),
      getSubtypeName(item.case_subtype_id),
    ]
      .join(" ")
      .toLowerCase()

    return searchableText.includes(keyword)
  })

  return (
    <PageLayout>
      <Topbar
        title="Perkara"
        eyebrow="MANAJEMEN PERKARA"
        action={
          <button className="primary-button" onClick={openAddModal}>
            + Tambah Perkara
          </button>
        }
      />

      <section className="page-intro">
        <p className="eyebrow">ARSIP PERKARA</p>

        <h2>Daftar Perkara</h2>

        <p>
          Setiap perkara menjadi tempat utama penyimpanan seluruh
          berkas klien.
        </p>
      </section>

      <section className="document-toolbar">
        <div className="document-search">
          <Search size={19} />

          <input
            type="text"
            placeholder="Cari klien, nomor perkara, pengadilan..."
            value={search}
            onChange={(event) => setSearch(event.target.value)}
          />
        </div>
      </section>

      {error && !showModal ? (
        <div className="client-form-error">{error}</div>
      ) : null}

      <section className="document-card">
        <div className="document-card-header">
          <div>
            <h3>Daftar Perkara</h3>
            <span>
              {loading
                ? "Memuat data perkara..."
                : `${filteredCases.length} perkara tersimpan`}
            </span>
          </div>
        </div>

        {loading ? (
          <div className="empty-state">
            <h4>Memuat perkara...</h4>
          </div>
        ) : filteredCases.length === 0 ? (
          <div className="empty-state">
            <div className="empty-icon">
              <BriefcaseBusiness size={30} />
            </div>

            <h4>Belum ada perkara</h4>

            <p>
              Buat perkara pertama untuk mulai menyiapkan lemari
              berkas digital.
            </p>

            <button
              className="primary-button"
              onClick={openAddModal}
            >
              + Tambah Perkara
            </button>
          </div>
        ) : (
          <div className="clients-table-wrap">
            <table className="clients-table">
              <thead>
                <tr>
                  <th>Klien</th>
                  <th>Nomor Perkara</th>
                  <th>Pengadilan</th>
                  <th>Jenis Perkara</th>
                  <th>Subjenis</th>
                  <th>Status</th>
                </tr>
              </thead>

              <tbody>
                {filteredCases.map((item) => (
                  <tr
                    key={item.id}
                    className="case-row"
                    onClick={() => navigate(`/berkas/${item.id}`)}
                  >
                    <td>
                      <strong>{getClientName(item.client_id)}</strong>
                      <small>{item.title}</small>
                    </td>

                    <td>{item.case_number}</td>

                    <td>
                      <strong>{getCourtName(item.court_id)}</strong>
                      <small>{getCourtTypeName(item.court_type_id)}</small>
                    </td>

                    <td>{getCaseTypeName(item.case_type_id)}</td>

                    <td>{getSubtypeName(item.case_subtype_id)}</td>

                    <td>
                      <span className="case-status">
                        {item.status || "Aktif"}
                      </span>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}
      </section>

      {showModal ? (
        <div className="modal-backdrop" onMouseDown={closeModal}>
          <div
            className="client-modal case-modal"
            onMouseDown={(event) => event.stopPropagation()}
          >
            <div className="client-modal-header">
              <div>
                <p className="eyebrow">PERKARA BARU</p>
                <h3>Tambah Perkara</h3>
              </div>

              <button
                type="button"
                className="modal-close"
                onClick={closeModal}
                disabled={saving}
                aria-label="Tutup"
              >
                ×
              </button>
            </div>

            <form className="client-form" onSubmit={handleSubmit}>
              <label>
                Klien
                <select
                  value={selectedClientId}
                  onChange={(event) =>
                    setSelectedClientId(event.target.value)
                  }
                  disabled={saving}
                >
                  <option value="">Pilih klien</option>
                  {clients.map((client) => (
                    <option key={client.id} value={client.id}>
                      {client.name}
                    </option>
                  ))}
                </select>
              </label>

              <label>
                Lingkungan Peradilan
                <select
                  value={selectedEnvironmentId}
                  onChange={(event) =>
                    handleEnvironmentChange(event.target.value)
                  }
                  disabled={saving}
                >
                  <option value="">Pilih lingkungan peradilan</option>
                  {environments.map((item) => (
                    <option key={item.id} value={item.id}>
                      {item.name}
                    </option>
                  ))}
                </select>
              </label>

              <label>
                Tipe Pengadilan
                <select
                  value={selectedCourtTypeId}
                  onChange={(event) =>
                    handleCourtTypeChange(event.target.value)
                  }
                  disabled={!selectedEnvironmentId || saving}
                >
                  <option value="">Pilih tipe pengadilan</option>
                  {filteredCourtTypes.map((item) => (
                    <option key={item.id} value={item.id}>
                      {item.name}
                    </option>
                  ))}
                </select>
              </label>

              <label>
                Pengadilan
                <select
                  value={selectedCourtId}
                  onChange={(event) =>
                    setSelectedCourtId(event.target.value)
                  }
                  disabled={!selectedCourtTypeId || saving}
                >
                  <option value="">Pilih pengadilan</option>
                  {filteredCourts.map((item) => (
                    <option key={item.id} value={item.id}>
                      {item.name}
                    </option>
                  ))}
                </select>
              </label>

              <label>
                Jenis Perkara
                <select
                  value={selectedCaseTypeId}
                  onChange={(event) =>
                    handleCaseTypeChange(event.target.value)
                  }
                  disabled={!selectedCourtTypeId || saving}
                >
                  <option value="">Pilih jenis perkara</option>
                  {filteredCaseTypes.map((item) => (
                    <option key={item.id} value={item.id}>
                      {item.name}
                    </option>
                  ))}
                </select>
              </label>

              <label>
                Subjenis Perkara
                <select
                  value={selectedSubtypeId}
                  onChange={(event) =>
                    setSelectedSubtypeId(event.target.value)
                  }
                  disabled={!selectedCaseTypeId || saving}
                >
                  <option value="">Pilih subjenis (opsional)</option>
                  {filteredSubtypes.map((item) => (
                    <option key={item.id} value={item.id}>
                      {item.name}
                    </option>
                  ))}
                </select>
              </label>

              <label>
                Nomor Perkara
                <input
                  type="text"
                  placeholder="Contoh: 123/Pdt.G/2026/PN Mtr"
                  value={caseNumber}
                  onChange={(event) =>
                    setCaseNumber(event.target.value)
                  }
                  disabled={saving}
                />
              </label>

              <label>
                Judul Perkara
                <input
                  type="text"
                  placeholder="Contoh: Gugatan Perdata Heri"
                  value={caseTitle}
                  onChange={(event) =>
                    setCaseTitle(event.target.value)
                  }
                  disabled={saving}
                />
              </label>

              {error ? (
                <div className="client-form-error">{error}</div>
              ) : null}

              <div className="client-form-actions">
                <button
                  type="button"
                  className="secondary-button"
                  onClick={closeModal}
                  disabled={saving}
                >
                  Batal
                </button>

                <button
                  type="submit"
                  className="primary-button"
                  disabled={saving}
                >
                  {saving ? "Menyimpan..." : "Simpan Perkara"}
                </button>
              </div>
            </form>
          </div>
        </div>
      ) : null}
    </PageLayout>
  )
}
function CaseArchiveIndex() {
  const { user } = useAuth()

  const [cases, setCases] = useState<
    Array<{
      id: string
      client_id: string
      court_id: string | null
      case_type_id: string | null
      case_subtype_id: string | null
      case_number: string
      title: string
      status: string
      created_at: string
    }>
  >([])

  const [clients, setClients] = useState<
    Array<{
      id: string
      name: string
    }>
  >([])

  const [courts, setCourts] = useState<
    Array<{
      id: string
      name: string
    }>
  >([])

  const [caseTypes, setCaseTypes] = useState<
    Array<{
      id: string
      name: string
    }>
  >([])

  const [caseSubtypes, setCaseSubtypes] = useState<
    Array<{
      id: string
      name: string
    }>
  >([])

  const [documentCounts, setDocumentCounts] = useState<
    Record<string, number>
  >({})

  const [search, setSearch] = useState("")
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState("")

  useEffect(() => {
    async function loadArchiveIndex() {
      if (!user) {
        setLoading(false)
        return
      }

      setLoading(true)
      setError("")

      const [
        casesResult,
        clientsResult,
        courtsResult,
        caseTypesResult,
        caseSubtypesResult,
        documentsResult,
      ] = await Promise.all([
        supabase
          .from("cases")
          .select(
            "id, client_id, court_id, case_type_id, case_subtype_id, case_number, title, status, created_at"
          )
          .eq("lawyer_id", user.id)
          .order("created_at", { ascending: false }),

        supabase
          .from("clients")
          .select("id, name")
          .eq("lawyer_id", user.id)
          .order("name"),

        supabase
          .from("courts")
          .select("id, name")
          .order("name"),

        supabase
          .from("case_types")
          .select("id, name")
          .order("name"),

        supabase
          .from("case_subtypes")
          .select("id, name")
          .order("name"),

        supabase
          .from("documents")
          .select("case_id, is_current")
          .eq("lawyer_id", user.id),
      ])

      const firstError =
        casesResult.error ??
        clientsResult.error ??
        courtsResult.error ??
        caseTypesResult.error ??
        caseSubtypesResult.error ??
        documentsResult.error

      if (firstError) {
        console.error("Gagal memuat indeks arsip:", firstError)
        setError(
          `Gagal memuat indeks arsip: ${firstError.message}`
        )
        setLoading(false)
        return
      }

      setCases((casesResult.data ?? []) as typeof cases)
      setClients((clientsResult.data ?? []) as typeof clients)
      setCourts((courtsResult.data ?? []) as typeof courts)
      setCaseTypes(
        (caseTypesResult.data ?? []) as typeof caseTypes
      )
      setCaseSubtypes(
        (caseSubtypesResult.data ?? []) as typeof caseSubtypes
      )

      const counts: Record<string, number> = {}

      for (const document of documentsResult.data ?? []) {
        if (!document.is_current) continue

        counts[document.case_id] =
          (counts[document.case_id] ?? 0) + 1
      }

      setDocumentCounts(counts)
      setLoading(false)
    }

    void loadArchiveIndex()
  }, [user])

  function getClientName(clientId: string) {
    return (
      clients.find((client) => client.id === clientId)?.name ??
      "Klien tidak ditemukan"
    )
  }

  function getCourtName(courtId: string | null) {
    if (!courtId) return "Pengadilan belum ditentukan"

    return (
      courts.find((court) => court.id === courtId)?.name ??
      "Pengadilan belum ditentukan"
    )
  }

  function getCaseTypeName(caseTypeId: string | null) {
    if (!caseTypeId) return ""

    return (
      caseTypes.find((type) => type.id === caseTypeId)?.name ??
      ""
    )
  }

  function getCaseSubtypeName(caseSubtypeId: string | null) {
    if (!caseSubtypeId) return ""

    return (
      caseSubtypes.find(
        (subtype) => subtype.id === caseSubtypeId
      )?.name ?? ""
    )
  }

  const filteredCases = cases.filter((item) => {
    const clientName = getClientName(item.client_id)
    const courtName = getCourtName(item.court_id)
    const caseTypeName = getCaseTypeName(item.case_type_id)
    const caseSubtypeName = getCaseSubtypeName(
      item.case_subtype_id
    )

    const haystack = [
      clientName,
      item.case_number,
      item.title,
      courtName,
      caseTypeName,
      caseSubtypeName,
    ]
      .join(" ")
      .toLowerCase()

    return haystack.includes(search.trim().toLowerCase())
  })

  return (
    <PageLayout>
      <Topbar
        title="Berkas"
        eyebrow="DIGITAL CASE FILE"
      />

      <section className="page-intro archive-index-intro">
        <p className="eyebrow">LEMARI ARSIP</p>

        <h2>Indeks Arsip Perkara</h2>

        <p>
          Temukan perkara terlebih dahulu, lalu buka lemari
          berkasnya.
        </p>
      </section>

      <section className="archive-index-toolbar">
        <div className="archive-index-search">
          <Search size={19} />

          <input
            type="text"
            value={search}
            onChange={(event) =>
              setSearch(event.target.value)
            }
            placeholder="Cari nama klien, nomor perkara, atau judul..."
            aria-label="Cari perkara"
          />

          {search ? (
            <button
              type="button"
              className="archive-index-clear"
              onClick={() => setSearch("")}
              aria-label="Hapus pencarian"
            >
              ×
            </button>
          ) : null}
        </div>
      </section>

      {loading ? (
        <section className="document-card">
          <div className="empty-state">
            <div className="empty-icon">
              <FolderOpen size={30} />
            </div>

            <h4>Memuat indeks arsip...</h4>

            <p>
              Mengambil daftar perkara dan jumlah berkas.
            </p>
          </div>
        </section>
      ) : error ? (
        <section className="document-card">
          <div className="empty-state">
            <div className="empty-icon">
              <FolderOpen size={30} />
            </div>

            <h4>Indeks arsip tidak dapat dimuat</h4>

            <p>{error}</p>
          </div>
        </section>
      ) : cases.length === 0 ? (
        <section className="document-card">
          <div className="empty-state">
            <div className="empty-icon">
              <BriefcaseBusiness size={30} />
            </div>

            <h4>Belum ada perkara</h4>

            <p>
              Buat perkara terlebih dahulu untuk mulai
              menyimpan berkas digital.
            </p>

            <NavLink
              to="/perkara"
              className="primary-button"
            >
              Buka Perkara
            </NavLink>
          </div>
        </section>
      ) : filteredCases.length === 0 ? (
        <section className="document-card">
          <div className="empty-state">
            <div className="empty-icon">
              <Search size={30} />
            </div>

            <h4>Perkara tidak ditemukan</h4>

            <p>
              Tidak ada perkara yang sesuai dengan pencarian
              "{search}".
            </p>

            <button
              type="button"
              className="secondary-button"
              onClick={() => setSearch("")}
            >
              Reset Pencarian
            </button>
          </div>
        </section>
      ) : (
        <section className="archive-index-list">
          <div className="archive-index-heading">
            <div>
              <p className="eyebrow">DAFTAR PERKARA</p>

              <h3>
                {filteredCases.length} perkara
              </h3>
            </div>

            <span>
              {search
                ? `Hasil pencarian untuk "${search}"`
                : "Semua perkara tersimpan"}
            </span>
          </div>

          <div className="archive-case-grid">
            {filteredCases.map((item) => {
              const clientName = getClientName(item.client_id)
              const courtName = getCourtName(item.court_id)
              const caseTypeName = getCaseTypeName(
                item.case_type_id
              )
              const caseSubtypeName =
                getCaseSubtypeName(item.case_subtype_id)

              const documentCount =
                documentCounts[item.id] ?? 0

              return (
                <article
                  key={item.id}
                  className="archive-case-card"
                >
                  <div className="archive-case-card-top">
                    <div className="archive-case-icon">
                      <FolderOpen size={22} />
                    </div>

                    <span className="archive-case-status">
                      {item.status}
                    </span>
                  </div>

                  <div className="archive-case-main">
                    <p className="eyebrow">KLIEN</p>

                    <h3>{clientName}</h3>

                    <strong>
                      {item.case_number}
                    </strong>

                    <p className="archive-case-title">
                      {item.title}
                    </p>
                  </div>

                  <div className="archive-case-meta">
                    <div>
                      <span>Pengadilan</span>
                      <strong>{courtName}</strong>
                    </div>

                    <div>
                      <span>Jenis Perkara</span>
                      <strong>
                        {caseTypeName || "-"}
                        {caseSubtypeName
                          ? ` • ${caseSubtypeName}`
                          : ""}
                      </strong>
                    </div>
                  </div>

                  <div className="archive-case-footer">
                    <div className="archive-case-count">
                      <FolderOpen size={16} />

                      <span>
                        {documentCount} berkas aktif
                      </span>
                    </div>

                    <NavLink
                      to={`/berkas/${item.id}`}
                      className="archive-case-open"
                    >
                      Buka Lemari
                      <span aria-hidden="true">→</span>
                    </NavLink>
                  </div>
                </article>
              )
            })}
          </div>
        </section>
      )}
    </PageLayout>
  )
}

function Documents() {
  const { caseId } = useParams()
  const { user } = useAuth()

  const [caseData, setCaseData] = useState<{
    id: string
    client_id: string
    case_number: string
    title: string
    court_id: string
    case_type_id: string
    case_subtype_id: string | null
  } | null>(null)

  const [clientName, setClientName] = useState("")
  const [courtName, setCourtName] = useState("")
  const [caseTypeName, setCaseTypeName] = useState("")
  const [subtypeName, setSubtypeName] = useState("")

  const [categories, setCategories] = useState<
    Array<{ id: string; name: string }>
  >([])

  const [documents, setDocuments] = useState<
    Array<{
      id: string
      category_id: string
      file_name: string
      file_extension: string
      file_size: number | null
      version: number
      is_current: boolean
      description: string | null
      storage_path: string
      created_at: string
    }>
  >([])

  const [loading, setLoading] = useState(true)
  const [error, setError] = useState("")

  const [showUploadModal, setShowUploadModal] = useState(false)
  const [uploadCategoryId, setUploadCategoryId] = useState("")
  const [uploadFile, setUploadFile] = useState<File | null>(null)
  const [uploadDescription, setUploadDescription] = useState("")
  const [uploading, setUploading] = useState(false)
  const [uploadError, setUploadError] = useState("")

  const [documentSearch, setDocumentSearch] = useState("")
  const [documentCategoryFilter, setDocumentCategoryFilter] = useState("all")
  const [documentFormatFilter, setDocumentFormatFilter] = useState("all")

  const [showPreview, setShowPreview] = useState(false)
  const [previewName, setPreviewName] = useState("")
  const [previewHtml, setPreviewHtml] = useState("")
  const [previewLoading, setPreviewLoading] = useState(false)
  const [previewError, setPreviewError] = useState("")

  useEffect(() => {
    async function loadCaseArchive() {
      if (!user || !caseId) {
        setLoading(false)
        return
      }

      setLoading(true)
      setError("")

      const { data: currentCase, error: caseError } = await supabase
        .from("cases")
        .select(
          "id, client_id, case_number, title, court_id, case_type_id, case_subtype_id"
        )
        .eq("id", caseId)
        .eq("lawyer_id", user.id)
        .single()

      if (caseError || !currentCase) {
        console.error(caseError)
        setError("Perkara tidak ditemukan atau tidak dapat diakses.")
        setLoading(false)
        return
      }

      setCaseData(currentCase)

      const [
        clientResult,
        courtResult,
        caseTypeResult,
        subtypeResult,
        categoriesResult,
        documentsResult,
      ] = await Promise.all([
        supabase
          .from("clients")
          .select("name")
          .eq("id", currentCase.client_id)
          .eq("lawyer_id", user.id)
          .single(),

        supabase
          .from("courts")
          .select("name")
          .eq("id", currentCase.court_id)
          .single(),

        supabase
          .from("case_types")
          .select("name")
          .eq("id", currentCase.case_type_id)
          .single(),

        currentCase.case_subtype_id
          ? supabase
              .from("case_subtypes")
              .select("name")
              .eq("id", currentCase.case_subtype_id)
              .single()
          : Promise.resolve({ data: null, error: null }),

        supabase
          .from("document_categories")
          .select("id, name")
          .order("name", { ascending: true }),

        supabase
          .from("documents")
          .select(
            "id, category_id, file_name, file_extension, file_size, version, is_current, description, storage_path, created_at"
          )
          .eq("case_id", caseId)
          .eq("lawyer_id", user.id)
          .order("created_at", { ascending: false }),
      ])

      const archiveError =
        clientResult.error ||
        courtResult.error ||
        caseTypeResult.error ||
        subtypeResult.error ||
        categoriesResult.error ||
        documentsResult.error

      if (archiveError) {
        console.error(archiveError)
        setError(
          `Gagal memuat lemari berkas: ${archiveError.message}`
        )
        setLoading(false)
        return
      }

      setClientName(clientResult.data?.name ?? "-")
      setCourtName(courtResult.data?.name ?? "-")
      setCaseTypeName(caseTypeResult.data?.name ?? "-")
      setSubtypeName(subtypeResult.data?.name ?? "")
      setCategories(categoriesResult.data ?? [])
      setDocuments(documentsResult.data ?? [])

      setLoading(false)
    }

    void loadCaseArchive()
  }, [user, caseId])

  function getCategoryDocuments(categoryId: string) {
    return documents.filter(
      (document) =>
        document.category_id === categoryId &&
        document.is_current
    )
  }

  function formatFileSize(size: number | null) {
    if (!size) return "-"

    if (size < 1024) {
      return `${size} B`
    }

    if (size < 1024 * 1024) {
      return `${(size / 1024).toFixed(1)} KB`
    }

    return `${(size / (1024 * 1024)).toFixed(1)} MB`
  }

  async function openDocument(document: {
    storage_path: string
    file_extension: string
    file_name?: string
  }) {
    const extension =
      document.file_extension.toLowerCase()

    setPreviewError("")
    setPreviewHtml("")
    setPreviewName(document.file_name ?? "Preview Dokumen")

    if (extension === "pdf") {
      const { data, error: signedUrlError } =
        await supabase.storage
          .from("case-files")
          .createSignedUrl(
            document.storage_path,
            60 * 10
          )

      if (signedUrlError || !data?.signedUrl) {
        console.error(signedUrlError)
        window.alert("Gagal membuka berkas PDF.")
        return
      }

      window.open(
        data.signedUrl,
        "_blank",
        "noopener,noreferrer"
      )

      return
    }

    if (extension === "docx") {
      setShowPreview(true)
      setPreviewLoading(true)

      try {
        const { data, error: signedUrlError } =
          await supabase.storage
            .from("case-files")
            .createSignedUrl(
              document.storage_path,
              60 * 10
            )

        if (signedUrlError || !data?.signedUrl) {
          throw (
            signedUrlError ||
            new Error("Gagal membuat link preview.")
          )
        }

        const response = await fetch(data.signedUrl)

        if (!response.ok) {
          throw new Error(
            `Gagal mengambil dokumen (${response.status}).`
          )
        }

        const arrayBuffer =
          await response.arrayBuffer()

        const result =
          await mammoth.convertToHtml({
            arrayBuffer,
          })

        setPreviewHtml(result.value)

        if (result.messages.length > 0) {
          console.warn(
            "Pesan dari Mammoth:",
            result.messages
          )
        }
      } catch (previewException) {
        console.error(previewException)

        setPreviewError(
          previewException instanceof Error
            ? previewException.message
            : "Gagal menampilkan preview dokumen."
        )
      } finally {
        setPreviewLoading(false)
      }

      return
    }

    window.alert(
      "Format DOC belum dapat dipreview langsung. Gunakan tombol Download untuk membuka dokumen."
    )
  }
  async function downloadDocument(document: {
    storage_path: string
    file_name: string
  }) {
    setError("")

    const { data, error: signedUrlError } =
      await supabase.storage
        .from("case-files")
        .createSignedUrl(document.storage_path, 60 * 10, {
          download: document.file_name,
        })

    if (signedUrlError || !data?.signedUrl) {
      console.error(signedUrlError)
      setError("Gagal membuat link download berkas.")
      return
    }

    const link = window.document.createElement("a")
    link.href = data.signedUrl
    link.download = document.file_name
    link.target = "_blank"
    link.rel = "noopener noreferrer"

    window.document.body.appendChild(link)
    link.click()
    link.remove()
  }

  async function deleteDocument(document: {
    id: string
    storage_path: string
    file_name: string
  }) {
    if (!user || !caseData) return

    const confirmed = window.confirm(
      `Hapus berkas "${document.file_name}"?\\n\\nFile akan dihapus dari lemari perkara dan Storage.`
    )

    if (!confirmed) return

    setError("")

    const { error: storageError } = await supabase.storage
      .from("case-files")
      .remove([document.storage_path])

    if (storageError) {
      console.error(storageError)
      setError(
        `Gagal menghapus file dari Storage: ${storageError.message}`
      )
      return
    }

    const { error: documentError } = await supabase
      .from("documents")
      .delete()
      .eq("id", document.id)
      .eq("case_id", caseData.id)
      .eq("lawyer_id", user.id)

    if (documentError) {
      console.error(documentError)
      setError(
        `File sudah dihapus dari Storage, tetapi data database gagal dihapus: ${documentError.message}`
      )
      return
    }

    setDocuments((current) =>
      current.filter((item) => item.id !== document.id)
    )
  }

  function openUploadModal(categoryId = "") {
    setUploadCategoryId(categoryId)
    setUploadFile(null)
    setUploadDescription("")
    setUploadError("")
    setShowUploadModal(true)
  }

  function closeUploadModal() {
    if (uploading) return

    setShowUploadModal(false)
    setUploadCategoryId("")
    setUploadFile(null)
    setUploadDescription("")
    setUploadError("")
  }

  async function handleUpload(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault()

    if (!user || !caseData || !uploadFile) {
      setUploadError("Pilih file terlebih dahulu.")
      return
    }

    if (!uploadCategoryId) {
      setUploadError("Pilih kategori berkas.")
      return
    }

    const originalName = uploadFile.name
    const extension =
      originalName.split(".").pop()?.toLowerCase() ?? ""

    const allowedExtensions = ["pdf", "doc", "docx"]

    if (!allowedExtensions.includes(extension)) {
      setUploadError(
        "Format file tidak diperbolehkan. Gunakan PDF, DOC, atau DOCX."
      )
      return
    }

    const allowedMimeTypes = [
      "application/pdf",
      "application/msword",
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    ]

    if (
      uploadFile.type &&
      !allowedMimeTypes.includes(uploadFile.type)
    ) {
      setUploadError(
        "Jenis file tidak sesuai. Gunakan PDF, DOC, atau DOCX."
      )
      return
    }

    setUploading(true)
    setUploadError("")

    try {
      /*
       * =====================================================
       * 1. Cari semua versi dokumen dengan nama yang sama
       * =====================================================
       */

      const { data: previousDocuments, error: previousError } =
        await supabase
          .from("documents")
          .select(
            "id, version, is_current, storage_path"
          )
          .eq("case_id", caseData.id)
          .eq("category_id", uploadCategoryId)
          .eq("lawyer_id", user.id)
          .eq("file_name", originalName)
          .order("version", { ascending: false })

      if (previousError) {
        throw previousError
      }

      const nextVersion =
        previousDocuments && previousDocuments.length > 0
          ? Math.max(
              ...previousDocuments.map(
                (document) => document.version
              )
            ) + 1
          : 1

      /*
       * =====================================================
       * 2. Buat nama file Storage berdasarkan versi
       * =====================================================
       */

      const baseName = originalName.replace(
        new RegExp(`\\.${extension}$`, "i"),
        ""
      )

      const safeBaseName = baseName.replace(
        /[^a-zA-Z0-9._-]/g,
        "_"
      )

      const uniqueName =
        nextVersion === 1
          ? `${safeBaseName}.${extension}`
          : `${safeBaseName}_v${nextVersion}.${extension}`

      const storagePath = [
        user.id,
        caseData.client_id,
        caseData.id,
        uploadCategoryId,
        uniqueName,
      ].join("/")

      /*
       * =====================================================
       * 3. Upload file baru ke Storage TERLEBIH DAHULU
       *
       * Jika gagal di sini, versi lama tetap aktif.
       * =====================================================
       */

      const { error: storageError } = await supabase.storage
        .from("case-files")
        .upload(storagePath, uploadFile, {
          cacheControl: "3600",
          upsert: false,
        })

      if (storageError) {
        throw storageError
      }

      /*
       * =====================================================
       * 4. Simpan record versi baru ke database
       * =====================================================
       */

      const { data: newDocument, error: documentError } =
        await supabase
          .from("documents")
          .insert({
            lawyer_id: user.id,
            client_id: caseData.client_id,
            case_id: caseData.id,
            category_id: uploadCategoryId,
            file_name: originalName,
            storage_path: storagePath,
            file_extension: extension,
            mime_type:
              uploadFile.type ||
              "application/octet-stream",
            file_size: uploadFile.size,
            description:
              uploadDescription.trim() || null,
            version: nextVersion,
            is_current: true,
          })
          .select("id")
          .single()

      /*
       * Jika insert database gagal,
       * hapus file baru dari Storage agar tidak menjadi orphan file.
       */

      if (documentError || !newDocument) {
        await supabase.storage
          .from("case-files")
          .remove([storagePath])

        throw (
          documentError ||
          new Error("Record dokumen gagal dibuat.")
        )
      }

      /*
       * =====================================================
       * 5. Setelah versi baru sukses tersimpan,
       *    baru tandai versi lama sebagai tidak aktif.
       * =====================================================
       */

      if (previousDocuments && previousDocuments.length > 0) {
        const { error: markOldError } = await supabase
          .from("documents")
          .update({ is_current: false })
          .eq("case_id", caseData.id)
          .eq("category_id", uploadCategoryId)
          .eq("lawyer_id", user.id)
          .eq("file_name", originalName)
          .neq("id", newDocument.id)

        if (markOldError) {
          /*
           * File dan record versi baru sudah berhasil dibuat.
           * Jangan menghapus file baru hanya karena update status
           * versi lama gagal. Laporkan error agar bisa diperbaiki.
           */
          throw markOldError
        }
      }

      /*
       * =====================================================
       * 6. Refresh daftar dokumen
       * =====================================================
       */

      const { data: refreshedDocuments, error: refreshError } =
        await supabase
          .from("documents")
          .select(
            "id, category_id, file_name, file_extension, file_size, version, is_current, description, storage_path, created_at"
          )
          .eq("case_id", caseData.id)
          .eq("lawyer_id", user.id)
          .order("created_at", { ascending: false })

      if (refreshError) {
        throw refreshError
      }

      setDocuments(refreshedDocuments ?? [])
      closeUploadModal()
    } catch (uploadException) {
      console.error(uploadException)

      const message =
        uploadException instanceof Error
          ? uploadException.message
          : "Gagal mengupload berkas."

      setUploadError(message)
    } finally {
      setUploading(false)
    }
  }
  if (!caseId) {
    return (
      <PageLayout>
        <Topbar
          title="Berkas"
          eyebrow="DIGITAL CASE FILE"
        />

        <section className="page-intro">
          <p className="eyebrow">LEMARI ARSIP</p>

          <h2>Pilih perkara terlebih dahulu.</h2>

          <p>
            Buka menu Perkara lalu pilih perkara untuk melihat
            lemari berkasnya.
          </p>
        </section>

        <section className="document-card">
          <div className="empty-state">
            <div className="empty-icon">
              <BriefcaseBusiness size={30} />
            </div>

            <h4>Belum ada perkara yang dipilih</h4>

            <NavLink to="/perkara" className="primary-button">
              Buka Daftar Perkara
            </NavLink>
          </div>
        </section>
      </PageLayout>
    )
  }

  const allDocuments = documents

  const filteredDocuments = allDocuments.filter((document) => {
    const search = documentSearch.trim().toLowerCase()

    const categoryName =
      categories.find(
        (category) => category.id === document.category_id
      )?.name ?? ""

    const matchesSearch =
      search === "" ||
      document.file_name.toLowerCase().includes(search) ||
      (document.description ?? "").toLowerCase().includes(search) ||
      categoryName.toLowerCase().includes(search)

    const matchesCategory =
      documentCategoryFilter === "all" ||
      document.category_id === documentCategoryFilter

    const extension =
      document.file_extension?.toLowerCase() ?? ""

    const matchesFormat =
      documentFormatFilter === "all" ||
      extension === documentFormatFilter

    return (
      matchesSearch &&
      matchesCategory &&
      matchesFormat
    )
  })

  return (
    <PageLayout>
      <Topbar
        title="Lemari Berkas"
        eyebrow="DIGITAL CASE FILE"
      />

      {loading ? (
        <section className="document-card">
          <div className="empty-state">
            <h4>Memuat lemari berkas...</h4>
          </div>
        </section>
      ) : error ? (
        <section className="document-card">
          <div className="empty-state">
            <div className="empty-icon">
              <FolderOpen size={30} />
            </div>

            <h4>Gagal memuat lemari</h4>

            <p>{error}</p>

            <NavLink to="/perkara" className="primary-button">
              Kembali ke Perkara
            </NavLink>
          </div>
        </section>
      ) : (
        <>
          <section className="case-archive-header">
            <div>
              <p className="eyebrow">LEMARI PERKARA</p>

              <h2>{clientName}</h2>

              <p className="case-archive-title">
                {caseData?.title}
              </p>
            </div>

            <div className="case-archive-meta">
              <div>
                <span>Nomor Perkara</span>
                <strong>{caseData?.case_number}</strong>
              </div>

              <div>
                <span>Pengadilan</span>
                <strong>{courtName}</strong>
              </div>

              <div>
                <span>Jenis</span>
                <strong>
                  {caseTypeName}
                  {subtypeName ? ` • ${subtypeName}` : ""}
                </strong>
              </div>
            </div>
          </section>

          <section className="page-intro archive-section-heading">
            <p className="eyebrow">KATEGORI BERKAS</p>

            <h2>Isi lemari perkara</h2>

            <p>
              Setiap kategori menjadi tempat penyimpanan berkas
              perkara yang terpisah.
            </p>
          </section>

          <section className="archive-category-grid">
            {categories.map((category) => {
              const categoryDocuments =
                getCategoryDocuments(category.id)

              return (
                <div
                  key={category.id}
                  className="archive-category-card"
                >
                  <div className="archive-category-icon">
                    <FolderOpen size={24} />
                  </div>

                  <div className="archive-category-content">
                    <h3>{category.name}</h3>

                    <p>
                      {categoryDocuments.length} berkas
                    </p>

                    {categoryDocuments.length > 0 ? (
                      <div className="archive-file-list">
                        {categoryDocuments
                          .slice(0, 3)
                          .map((document) => (
                            <div
                              key={document.id}
                              className="archive-file-item"
                            >
                              <FileText size={15} />

                              <span>{document.file_name}</span>
                            </div>
                          ))}

                        {categoryDocuments.length > 3 ? (
                          <small>
                            +
                            {categoryDocuments.length - 3} berkas
                            lainnya
                          </small>
                        ) : null}
                      </div>
                    ) : (
                      <div className="archive-empty-category">
                        Belum ada berkas
                      </div>
                    )}
                  </div>

                  <button
                    type="button"
                    className="archive-category-button"
                    onClick={() =>
                      openUploadModal(category.id)
                    }
                  >
                    Buka
                  </button>
                </div>
              )
            })}
          </section>

          <section className="document-card archive-all-files">
            <div className="document-card-header">
              <div>
                <h3>Ringkasan Berkas</h3>

                <span>
                  {filteredDocuments.length} berkas ditampilkan dari
                  {allDocuments.length} berkas
                </span>
              </div>

              <button
                className="primary-button"
                type="button"
                onClick={() => openUploadModal()}
              >
                <Upload size={15} />
                Upload Berkas
              </button>
            </div>

            <div className="archive-filter-toolbar">
              <div className="archive-filter-search">
                <Search size={18} />

                <input
                  type="text"
                  value={documentSearch}
                  onChange={(event) =>
                    setDocumentSearch(event.target.value)
                  }
                  placeholder="Cari nama berkas, deskripsi, atau kategori..."
                  aria-label="Cari berkas"
                />

                {documentSearch ? (
                  <button
                    type="button"
                    className="archive-filter-clear"
                    onClick={() => setDocumentSearch("")}
                    aria-label="Hapus pencarian"
                  >
                    ×
                  </button>
                ) : null}
              </div>

              <select
                className="archive-filter-select"
                value={documentCategoryFilter}
                onChange={(event) =>
                  setDocumentCategoryFilter(event.target.value)
                }
                aria-label="Filter kategori"
              >
                <option value="all">Semua Kategori</option>

                {categories.map((category) => (
                  <option
                    key={category.id}
                    value={category.id}
                  >
                    {category.name}
                  </option>
                ))}
              </select>

              <select
                className="archive-filter-select"
                value={documentFormatFilter}
                onChange={(event) =>
                  setDocumentFormatFilter(event.target.value)
                }
                aria-label="Filter format"
              >
                <option value="all">Semua Format</option>
                <option value="pdf">PDF</option>
                <option value="doc">DOC</option>
                <option value="docx">DOCX</option>
              </select>
            </div>

            {allDocuments.length === 0 ? (
              <div className="empty-state">
                <div className="empty-icon">
                  <FolderOpen size={30} />
                </div>

                <h4>Belum ada berkas</h4>

                <p>
                  Upload PDF, DOC, atau DOCX ke kategori yang sesuai.
                </p>

                <button
                  className="primary-button"
                  type="button"
                  onClick={() => openUploadModal()}
                >
                  <Upload size={15} />
                  Upload Berkas
                </button>
              </div>
            ) : filteredDocuments.length === 0 ? (
              <div className="empty-state archive-filter-empty">
                <div className="empty-icon">
                  <Search size={30} />
                </div>

                <h4>Berkas tidak ditemukan</h4>

                <p>
                  Tidak ada berkas yang sesuai dengan pencarian
                  atau filter yang dipilih.
                </p>

                <button
                  type="button"
                  className="secondary-button"
                  onClick={() => {
                    setDocumentSearch("")
                    setDocumentCategoryFilter("all")
                    setDocumentFormatFilter("all")
                  }}
                >
                  Reset Filter
                </button>
              </div>
            ) : (
              <div className="clients-table-wrap">
                <table className="clients-table">
                  <thead>
                    <tr>
                      <th>Nama Berkas</th>
                      <th>Kategori</th>
                      <th>Format</th>
                      <th>Ukuran</th>
                      <th>Versi</th>
                      <th>Aksi</th>
                    </tr>
                  </thead>

                  <tbody>
                    {filteredDocuments.map((document) => (
                      <tr key={document.id}>
                        <td>
                          <strong>{document.file_name}</strong>

                          {document.description ? (
                            <small>
                              {document.description}
                            </small>
                          ) : null}
                        </td>

                        <td>
                          {categories.find(
                            (category) =>
                              category.id === document.category_id
                          )?.name ?? "-"}
                        </td>

                        <td>
                          {document.file_extension.toUpperCase()}
                        </td>

                        <td>
                          {formatFileSize(document.file_size)}
                        </td>

                        <td>
                          <div className="document-version">
                            <strong>v{document.version}</strong>

                            {document.is_current ? (
                              <span className="document-version-current">
                                Aktif
                              </span>
                            ) : (
                              <span className="document-version-old">
                                Versi Lama
                              </span>
                            )}
                          </div>
                        </td>

                        <td>
                          <div className="document-actions">
                            <button
                              type="button"
                              className="document-action-button"
                              onClick={() =>
                                void openDocument(document)
                              }
                              title="Buka berkas"
                            >
                              <Eye size={14} />
                              <span>Buka</span>
                            </button>

                            <button
                              type="button"
                              className="document-action-button"
                              onClick={() =>
                                void downloadDocument(document)
                              }
                              title="Download berkas"
                            >
                              <Download size={14} />
                              <span>Download</span>
                            </button>

                            <button
                              type="button"
                              className="document-action-button document-action-danger"
                              onClick={() =>
                                void deleteDocument(document)
                              }
                              title="Hapus berkas"
                            >
                              <Trash2 size={14} />
                              <span>Hapus</span>
                            </button>
                          </div>
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            )}
          </section>

          {showPreview
  ? createPortal(
      <div
        onMouseDown={() => {
          if (!previewLoading) {
            setShowPreview(false)
          }
        }}
        style={{
          position: "fixed",
          inset: 0,
          zIndex: 2147483647,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          padding: "24px",
          background: "rgba(7, 15, 29, 0.78)",
          backdropFilter: "blur(6px)",
        }}
      >
        <div
          onMouseDown={(event) => event.stopPropagation()}
          style={{
            position: "relative",
            zIndex: 2147483647,
            display: "flex",
            flexDirection: "column",
            width: "min(1100px, 94vw)",
            height: "min(88vh, 900px)",
            maxHeight: "calc(100vh - 48px)",
            minHeight: "400px",
            overflow: "hidden",
            background: "#ffffff",
            color: "#172033",
            border: "1px solid #d8dee8",
            borderRadius: "20px",
            boxShadow: "0 30px 90px rgba(0, 0, 0, 0.45)",
          }}
        >
          <div
            style={{
              display: "flex",
              alignItems: "center",
              justifyContent: "space-between",
              gap: "20px",
              flexShrink: 0,
              padding: "20px 24px",
              background: "#ffffff",
              color: "#172033",
              borderBottom: "1px solid #e5e9ef",
            }}
          >
            <div>
              <p
                style={{
                  margin: 0,
                  fontSize: "11px",
                  fontWeight: 700,
                  letterSpacing: "0.12em",
                  color: "#b58a2b",
                }}
              >
                PREVIEW DOKUMEN
              </p>

              <h3
                style={{
                  margin: "5px 0 0",
                  fontSize: "18px",
                  lineHeight: 1.4,
                  color: "#172033",
                  wordBreak: "break-word",
                }}
              >
                {previewName}
              </h3>
            </div>

            <button
              type="button"
              onClick={() => setShowPreview(false)}
              disabled={previewLoading}
              aria-label="Tutup preview"
              style={{
                width: "38px",
                height: "38px",
                flexShrink: 0,
                border: "1px solid #d8dee8",
                borderRadius: "10px",
                background: "#ffffff",
                color: "#172033",
                fontSize: "24px",
                lineHeight: 1,
                cursor: previewLoading ? "not-allowed" : "pointer",
              }}
            >
              ×
            </button>
          </div>

          <div
            style={{
              flex: 1,
              minHeight: 0,
              overflow: "auto",
              padding: "32px",
              background: "#f3f4f6",
            }}
          >
            {previewLoading ? (
              <div
                style={{
                  minHeight: "100%",
                  display: "flex",
                  flexDirection: "column",
                  alignItems: "center",
                  justifyContent: "center",
                  textAlign: "center",
                  color: "#172033",
                }}
              >
                <div
                  style={{
                    width: "64px",
                    height: "64px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "center",
                    marginBottom: "18px",
                    borderRadius: "16px",
                    background: "#f1eadb",
                    color: "#b58a2b",
                  }}
                >
                  <FileText size={30} />
                </div>

                <h4
                  style={{
                    margin: "0 0 8px",
                    fontSize: "18px",
                    color: "#172033",
                  }}
                >
                  Menyiapkan preview...
                </h4>

                <p
                  style={{
                    margin: 0,
                    color: "#667085",
                  }}
                >
                  Dokumen sedang diproses untuk ditampilkan.
                </p>
              </div>
            ) : previewError ? (
              <div
                style={{
                  minHeight: "100%",
                  display: "flex",
                  flexDirection: "column",
                  alignItems: "center",
                  justifyContent: "center",
                  textAlign: "center",
                  color: "#172033",
                }}
              >
                <div
                  style={{
                    width: "64px",
                    height: "64px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "center",
                    marginBottom: "18px",
                    borderRadius: "16px",
                    background: "#f5e8e8",
                    color: "#b42318",
                  }}
                >
                  <FileText size={30} />
                </div>

                <h4
                  style={{
                    margin: "0 0 8px",
                    fontSize: "18px",
                    color: "#172033",
                  }}
                >
                  Preview tidak tersedia
                </h4>

                <p
                  style={{
                    maxWidth: "600px",
                    margin: 0,
                    color: "#667085",
                  }}
                >
                  {previewError}
                </p>
              </div>
            ) : (
              <article
                style={{
                  width: "min(820px, 100%)",
                  minHeight: "100%",
                  boxSizing: "border-box",
                  margin: "0 auto",
                  padding: "56px 64px",
                  background: "#ffffff",
                  color: "#1f2937",
                  lineHeight: 1.75,
                  boxShadow: "0 8px 30px rgba(15, 23, 42, 0.08)",
                }}
                dangerouslySetInnerHTML={{
                  __html: previewHtml,
                }}
              />
            )}
          </div>
        </div>
      </div>,
      document.body
    )
  : null}
          {showUploadModal ? (
            <div
              className="modal-backdrop"
              onMouseDown={closeUploadModal}
            >
              <div
                className="client-modal case-modal upload-modal"
                onMouseDown={(event) =>
                  event.stopPropagation()
                }
              >
                <div className="client-modal-header">
                  <div>
                    <p className="eyebrow">ARSIP DIGITAL</p>
                    <h3>Upload Berkas</h3>
                  </div>

                  <button
                    type="button"
                    className="modal-close"
                    onClick={closeUploadModal}
                    disabled={uploading}
                    aria-label="Tutup"
                  >
                    ×
                  </button>
                </div>

                <form
                  className="client-form"
                  onSubmit={handleUpload}
                >
                  <label>
                    Kategori Berkas
                    <select
                      value={uploadCategoryId}
                      onChange={(event) =>
                        setUploadCategoryId(
                          event.target.value
                        )
                      }
                      disabled={uploading}
                    >
                      <option value="">
                        Pilih kategori berkas
                      </option>

                      {categories.map((category) => (
                        <option
                          key={category.id}
                          value={category.id}
                        >
                          {category.name}
                        </option>
                      ))}
                    </select>
                  </label>

                  <label>
                    File
                    <input
                      type="file"
                      accept=".pdf,.doc,.docx,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document"
                      onChange={(event) =>
                        setUploadFile(
                          event.target.files?.[0] ?? null
                        )
                      }
                      disabled={uploading}
                    />
                  </label>

                  {uploadFile ? (
                    <div className="upload-file-preview">
                      <FileText size={18} />

                      <div>
                        <strong>{uploadFile.name}</strong>

                        <small>
                          {formatFileSize(uploadFile.size)}
                        </small>
                      </div>
                    </div>
                  ) : null}

                  <label>
                    Keterangan
                    <textarea
                      placeholder="Opsional. Contoh: Bukti transfer tanggal 12 Januari 2026."
                      value={uploadDescription}
                      onChange={(event) =>
                        setUploadDescription(
                          event.target.value
                        )
                      }
                      disabled={uploading}
                      rows={4}
                    />
                  </label>

                  <div className="upload-format-info">
                    Format yang diperbolehkan:
                    <strong> PDF, DOC, DOCX</strong>
                  </div>

                  {uploadError ? (
                    <div className="client-form-error">
                      {uploadError}
                    </div>
                  ) : null}

                  <div className="client-form-actions">
                    <button
                      type="button"
                      className="secondary-button"
                      onClick={closeUploadModal}
                      disabled={uploading}
                    >
                      Batal
                    </button>

                    <button
                      type="submit"
                      className="primary-button"
                      disabled={uploading}
                    >
                      <Upload size={15} />

                      {uploading
                        ? "Mengupload..."
                        : "Upload Berkas"}
                    </button>
                  </div>
                </form>
              </div>
            </div>
          ) : null}
        </>
      )}
    </PageLayout>
  )
}
function ProtectedRoute({ children }: { children: ReactNode }) {
  const { user, loading } = useAuth()

  if (loading) {
    return <div className="auth-loading">Memuat...</div>
  }

  if (!user) {
    return <Navigate to="/login" replace />
  }

  return <>{children}</>
}

function LoginRoute() {
  const { user, loading } = useAuth()

  if (loading) {
    return <div className="auth-loading">Memuat...</div>
  }

  if (user) {
    return <Navigate to="/" replace />
  }

  return <Login />
}

function App() {
  return (
    <AuthProvider>
      <BrowserRouter>
        <Routes>
          <Route path="/login" element={<LoginRoute />} />

          <Route
            path="/"
            element={
              <ProtectedRoute>
                <Dashboard />
              </ProtectedRoute>
            }
          />

          <Route
            path="/klien"
            element={
              <ProtectedRoute>
                <Clients />
              </ProtectedRoute>
            }
          />

          <Route
            path="/perkara"
            element={
              <ProtectedRoute>
                <Cases />
              </ProtectedRoute>
            }
          />

          <Route
            path="/berkas"
            element={
              <ProtectedRoute>
                <CaseArchiveIndex />
              </ProtectedRoute>
            }
          />

          <Route
            path="/berkas/:caseId"
            element={
              <ProtectedRoute>
                <Documents />
              </ProtectedRoute>
            }
          />

          <Route path="*" element={<Navigate to="/" replace />} />
        </Routes>
      </BrowserRouter>
    </AuthProvider>
  )
}
export default App






































